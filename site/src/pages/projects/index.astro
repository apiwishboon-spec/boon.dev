---
import Base from "../../layouts/Base.astro";
---
<Base title="Projects | Boon" description="Selected work and experiments by Boon.">

  <header class="page-head">
    <span class="kicker"><i class="fa-solid fa-star"></i> MISSION LOG</span>
    <h1>The <span class="grad-text">Work</span></h1>
    <p>Selected missions launched from the lab.</p>
  </header>

  <section class="section">
    <div class="container">
      <div class="grid grid-2" id="projects"></div>
    </div>
  </section>
</Base>

<script>
  import { bootSite, escapeHtml } from "../../lib/site-loader";
  import type { AllData } from "../../lib/types";

  bootSite((data: AllData) => {
    const wrap = document.getElementById("projects");
    if (!wrap) return;
    wrap.innerHTML = data.projects.map((p) => `
      <a class="glass project-card" href="/projects/${encodeURIComponent(p.slug)}" style="color:inherit;text-decoration:none;">
        <div class="pc-icon"><i class="fa-solid ${p.tech?.includes("AI") ? "fa-robot" : "fa-gears"}"></i></div>
        <h3>${escapeHtml(p.title)}</h3>
        <p>${escapeHtml(p.tagline || p.description)}</p>
        <div class="tagrow">${(p.tech || []).map(t => `<span class="chip"><i class="fa-solid fa-tag"></i>${escapeHtml(t)}</span>`).join("")}</div>
      </a>`).join("");
  });
</script>
